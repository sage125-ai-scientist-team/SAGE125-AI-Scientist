# SAGE125 Initial Repository Baseline

**Date:** 2026-07-22  
**Status:** Initial audited import with known test failures; not a release build.

## Source of this record

The results below were reported by the local Codex review performed before
the first Git commit. They are recorded as the pre-sprint baseline and must
not be represented as a fully passing test suite.

## Security and repository preparation

- The real `.env` was not read, printed, staged, or committed.
- Static long-form `sk-` test fixtures were replaced with runtime-generated
  test values.
- Rescan result: 0 continuous `sk-`-style secret hits.
- Rescan result: 0 other high-risk credential-format hits.
- Rescan result: 0 forbidden-path candidates.
- `sjtu-booklet.pdf` and `data/raw/sjtu-booklet.pdf` are local external
  inputs and are excluded from Git.
- The two local PDF copies were reported as byte-identical:
  - Size: 8,422,081 bytes each
  - SHA-256 prefix: `4bda50e8e3c9`
- The booklet is problem input only and must not be treated as scholarly
  evidence supporting scientific claims.

## Test results reported by Codex

### Focused security-related tests

Command scope:

- `tests/test_audit_project.py`
- `tests/test_embedding_error_mapping.py`
- `tests/test_ui_errors_contract.py`

Result:

- 25 passed
- 1 failed

Reported failure:

- Existing agent guidance text did not match the variable-name wording
  required by the assertion.

### Configuration and user-guide tests

Scope:

- `tests/test_config.py`
- `tests/test_user_guide_scope.py`

Result:

- 8 passed
- 1 warning

### Full test suite

Result:

- 208 passed
- 35 skipped
- 7 failed
- 2 warnings

Reported environment or fixture limitations included:

- Doctor report absent from the isolated test mirror.
- Problem inventory absent from the isolated test mirror.
- PDF inputs intentionally absent from the isolated test mirror.
- `.streamlit/config.toml` absent from the isolated test mirror.
- Export/cache paths had write-permission limitations.
- One existing agent-guidance assertion mismatch remained.

## Interpretation

This commit is an auditable starting point, not evidence that all tests pass.
All seven failures must be classified as one of:

1. a real product defect;
2. a test-environment defect;
3. a missing fixture or documented external input;
4. an obsolete or incorrect assertion.

Future work must not delete or weaken tests merely to obtain a green result.
