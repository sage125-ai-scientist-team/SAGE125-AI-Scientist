# T09 PR-A second-review audit notes

## Scope and provenance

This note records the second-review remediation on `t09/a-quality-contract`.

| Item | Value |
| --- | --- |
| Reviewed previous head | `a0d3f2a244c9f11d346328a782dec4e4cb0cc8db` |
| Integration baseline | `1642ea05e88b853f18d24739d9d2134c3448eb7b` |
| CI implementation commit | `44cec54b7993aca850230c72a16510bf79009774` |
| PR state | Draft; no merge or Ready transition was performed. |

The PR changes the T09-owned workflow, offline integration fixture, evaluation scripts, and reproducibility documents. No Wave B work, production secret, live model call, or 125-question batch run is in scope.

## Second-review remediation

- Added `ready_for_review` to the pull-request trigger while retaining the integration base branch, minimum `contents: read` permissions, offline Mock mode, and global UTF-8 environment.
- Preserved `python -X utf8` on every Python command in all six fixed jobs.
- Added six stable, real evidence artifacts generated by the commands that the jobs execute; uploads use `if: always()` only to preserve failure evidence and use `if-no-files-found: error`.
- Replaced stale baseline counts with clean-worktree evidence: private question list and PDF were absent, so skips are explicit rather than hidden.
- Replaced the release-manifest statement that used the obsolete `450551f`, `249 passed / 1 failed`, and PR #1 Ready dependency claim.

## Local evidence

In a temporary clean Git worktree at the integration-derived source, with Python 3.12.10, `MOCK_LLM=true`, `PYTHONUTF8=1`, and `PYTHONIOENCODING=utf-8`:

- lint and type exited 0 with no contract failures;
- unit exited 0: 237 passed, 35 skipped;
- integration exited 0: 1 passed;
- security exited 0: 0 critical findings and 1 warning;
- build compilation, benchmark dry-run, and schema validation all exited 0;
- full pytest exited 0: 238 passed, 35 skipped.

The skips are due to intentionally absent `questions_125.json` and `data/raw/sjtu-booklet.pdf`, not a new skip mechanism. The exact commands, counts, and temporary artifact paths are in `T09_QUALITY_BASELINE.md`.

## GitHub Actions status

The CI implementation commit has not yet been observed in GitHub Actions at the time of this evidence edit. Its six check states, run URLs, and artifact URLs are **pending** until the normal push is made and GitHub reports them. This note does not mark any GitHub check, captain review, P1, or P2 as closed.
