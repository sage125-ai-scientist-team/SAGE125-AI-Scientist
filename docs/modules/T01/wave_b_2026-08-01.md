# T01 Wave B — 08/01 引用展示与 Draft PR-B

## Sync note

- PR-A #7 squash-merged into `integration/2026-08-10` (`94cd434`)
- Branch `t01/b-evidence-core` rebased / up to date with integration tip (`behind=0`)
- Draft PR-B: #13（保持 Draft，未 Ready）

## Handbook requirement

实现报告/API 中的可点击来源、页码/章节、原文片段和支持状态；向 T08 提供 payload；扩展黄金集并开启 Draft PR-B。

## Implementation

| Path | Role |
|---|---|
| `app/evidence/citation_renderer.py` | Markdown 跳转 + T08 JSON payload |
| `app/evidence/gold_set.py` | 黄金集 JSON 加载器 |
| `docs/modules/T01/evidence_gold_set.json` | 20 → **24** provisional pairs |
| `tests/evidence/test_citation_renderer.py` | 渲染 / payload / 金标加载测试 |

## Upstream ref recorded in payload

`integration/2026-08-10 + t01/b-evidence-core`

## Verify

```powershell
python -m pytest -q tests/evidence -vv
```
