# T01 Wave B — 08/03 跨模块联调与接口冻结

## Deliverables

| Artifact | Path |
|---|---|
| Integration bridge | `app/evidence/integration_bridge.py` |
| E2E tests | `tests/evidence/test_integration_e2e.py` |
| Interface freeze | `docs/modules/T01/interface_freeze_2026-08-03.md` |
| API contract note | `docs/contracts/T01.md` §6.5 |

## Acceptance (handbook 08/03)

| Scenario | Expected | Status |
|---|---|---|
| V1/V2 版本记录 | Bundle 指纹写入 `PlanVersion.prompt_fingerprints`，序列化往返不丢 | covered |
| 缺证据 | 未知 `evidence_id` → `T01_MISSING_EVIDENCE` P1，gate fail | covered |
| 冲突证据 | 同 claim 同时 `supports`+`contradicts` → `T01_CONFLICT_EVIDENCE` P1 | covered |
| 字段不丢失 | `quoted_text` / `locator` / `content_hash` 投影到 T03 后仍在 | covered |

## Reproduce

```powershell
pytest -q tests/evidence/test_integration_e2e.py
```

## Notes

- 不修改 `app/workflow/pipeline.py`（T02 所有权）。
- 不修改 T02/T03 owner 实现；仅消费已合并契约（`PlanVersion` / `ValidationContext` / `GateFinding`）。
- 完整 Validator 业务规则仍由 T03 `ValidationService` 负责；本日落点是交接桥 + E2E 夹具。
- T03-A 已合入 `integration/2026-08-10` 后方可联调。
