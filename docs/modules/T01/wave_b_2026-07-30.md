# T01 Wave B — 07/30 全文上下文构建

## Status

Branch: `t01/b-evidence-core`  
Date: 2026-07-30  
Deliverable: EvidenceBundle builder

## Handbook requirement

从最新 integration 新建 B 分支；实现 quote、页码/章节、作者、年份、来源类型与内容哈希进入受控上下文；记录截断原因、token 预算和缺失字段。

验收：`quoted_text` 不再被删除；输入可审计且不暴露无关全文。

## Implementation

| Path | Role |
|---|---|
| `app/evidence/bundle_builder.py` | `build_evidence_bundle` / `runtime_card_to_contract` / `bundle_to_agent_context` |
| `app/evidence/__init__.py` | 公共导出 |
| `tests/evidence/test_bundle_builder.py` | Wave B 日验收测试 |
| `app/contracts/evidence.py` | Wave A 冻结契约（本 PR 不改 pipeline） |

## Contrast with `_evidence_catalog`

| Field | `_evidence_catalog` (pipeline) | Bundle builder |
|---|---|---|
| quoted_text | dropped | **kept** |
| authors | dropped | kept |
| locator | absent | parsed / override / inferred |
| content_hash | absent | sha256 of quote |
| truncation | hard limit=30 cards | token_budget + reason |

## Scope boundary

- 不修改 `app/workflow/pipeline.py`（T02 接入）。
- PR-A (#7) 已 Approve；合并需队长权限。B 分支暂基于已批准的 A tip，待 A squash 进 integration 后 rebase。

## Verify

```powershell
python -m pytest -q tests/evidence -vv
```
