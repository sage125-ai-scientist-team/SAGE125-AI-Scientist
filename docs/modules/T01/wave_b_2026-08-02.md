# T01 Wave B — 08/02 Q028 回归与审查

## Handbook requirement

运行 Q028 回归，比较修复前后；处理 Codex 与 T04 配对审查；优化片段去重、排序、截断和错误信息。

## Deliverables

| Artifact | Path |
|---|---|
| Q028 runner | `app/evidence/q028_regression.py` |
| Report v1 | `docs/modules/T01/q028_regression_v1.md` |
| Tests | `tests/evidence/test_q028_regression.py` |
| Builder hardening | `app/evidence/bundle_builder.py`（排序 / 去重 / 截断信息） |

## Acceptance evidence

| Gate | Result |
|---|---|
| 不存在虚构 `booklet_excerpt_Q028` | `FAKE_BOOKLET_EVIDENCE_ID` → BLOCK |
| 问题册不得支撑 established facts | `BOOKLET_EXCLUDED` → BLOCK |
| 单一癌种 ↛ 所有癌症 | `OVERGENERALIZATION` → DEGRADE |
| 无支持结论不得伪装 allow | 回归矩阵全 PASS |

## Scope honesty（契约层）

- 本日为 **contract-layer / support-checker** 回归，**不是** live `pipeline.py` 全链路 Q028 跑题。
- 不修改 `app/workflow/pipeline.py`（T02 所有权）。
- 全链路 agent-trace 联调见 08/03 `integration_bridge`（仍不改 pipeline）。

## Pairing / review process

| Item | Status |
|---|---|
| Codex / captain review | 在 Draft PR #13 请求；Ready 目标仍为 08/05 |
| T04 配对审查 | 已在 PR #13 正式请求（booklet 边界与 RAG 对齐） |
| 审查修订提交 | 见本分支后续 push（含本日报告与 builder 硬化） |

## Reproduce

```powershell
python -c "from app.evidence.q028_regression import run_q028_regression; print(run_q028_regression().to_dict())"
python -m pytest -q tests/evidence/test_q028_regression.py
```
