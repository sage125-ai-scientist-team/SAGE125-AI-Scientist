# T01 Wave C — 2026-08-06 质量门加固

**Branch:** `t01/c-evidence-hardening`  
**Base integration HEAD (branch cut):** see PR body / sync report  
**Requirements:** T01-C-001 / T01-C-002 / T01-C-003  
**PR status target:** OPEN + **Draft**（不自行 Ready / Merge）

## Deliverables

| Item | Path |
|---|---|
| 冲突/反例质量门 | `app/evidence/quality_gate.py` |
| 内容哈希缓存 + 确定性 | `app/evidence/content_hash_cache.py` |
| 测试 | `tests/evidence/test_quality_gate.py` |
| 测试 | `tests/evidence/test_content_hash_cache.py` |

## Acceptance evidence

1. **冲突不被静默覆盖**：`detect_conflicts_preserving_both_sides` 同时保留 supports 与 contradicts 两侧 evidence_id；任一侧为空则抛错。
2. **撤稿/来源状态占位**：默认 `PLACEHOLDER`；`RETRACTED`/`WITHDRAWN` 进入 `retracted_blocked_ids`，不得伪装 ACTIVE。
3. **同输入证据集合稳定**：`assert_same_input_stable_evidence_set` + `deterministic_bundle_digest`。

## Reproduce

```powershell
python -m pytest -q tests/evidence/test_quality_gate.py tests/evidence/test_content_hash_cache.py
```

## Boundaries

- 不修改 `app/workflow/pipeline.py`
- 不改冻结 Wave A `app/contracts/evidence.py` 字段语义
- T04 retrieval pairing 边界保持：`STRUCTURE_OK` / `ACTUAL_RELEVANCE_GOLD=NOT_READY` / `FORMAL_RETRIEVAL_METRICS_AUTHORIZED=false`
- 旧 PR #25 provenance ACCEPT_CANDIDATE 为历史结论；本 C 分支新 HEAD 需 T09 重新复验
