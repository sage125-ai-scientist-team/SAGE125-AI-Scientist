# T01 Wave B — 08/04 指标与跨领域抽查

## Deliverables

| Artifact | Path |
|---|---|
| Gold set (30) | `docs/modules/T01/evidence_gold_set.json` |
| Metrics | `docs/modules/T01/metrics.json` |
| 12-domain audit | `docs/modules/T01/domain_audit_12.json` |
| Calculator | `app/evidence/metrics.py` |
| Tests | `tests/evidence/test_metrics.py` |

## Metrics (regenerated)

Command:

```powershell
# 只读计算（不写盘）
python -c "from app.evidence.metrics import compute_metrics; print(compute_metrics(generated_at='2026-08-02T06:41:57.966684+00:00').to_dict())"

# 刷新仓库已跟踪审计产物（须显式 opt-in；pytest 禁止走此路径，见 Issue #44）
python -c "from app.evidence.metrics import generate_wave_b_metrics_artifacts; print(generate_wave_b_metrics_artifacts(allow_tracked_write=True, generated_at='2026-08-02T06:41:57.966684+00:00'))"
```

Acceptance:

- `gold_set_size >= 30`
- `support_precision >= 0.90` (evaluated on predicted ALLOW)
- uncertain cases use explicit DEGRADE/BLOCK strategy (never fake ALLOW)
- 12-domain table includes Q028 overgeneralization policy

## Notes

- Synced `upstream/integration/2026-08-10` before this day (includes merged T03-A contracts).
- T01↔T02/T03 contract-layer E2E is covered by `wave_b_2026-08-03.md` + `test_integration_e2e.py` (not full pipeline; pipeline owned by T02).
- Gold set: `provisional=true` + `label_tier=wave_b_manual_fixture_accepted`（Wave B 夹具验收）；独立科学精确率 / live-trace 签字 **明确留给 Wave C**（见 JSON `annotation_policy.deferred_to_wave_c`）。
- `support_precision` is **fixture accuracy vs hand-assigned expected_decision**, not an independent scientific annotator ≥90% score (`metrics.json`: `not_independent_scientific_precision=true`).
- `domain_audit_12` is a representative policy sampling table with `linked_gold_claim_ids`, **not** live agent-trace runs.
- Bridge-only / UI scope written acceptance: `wave_b_scope_acceptance.md`（T01-B-009/B-013/B-015）。
- Captain reviews: DOI-only P0 closed; see `captain_response_2026-08-01.md`.
