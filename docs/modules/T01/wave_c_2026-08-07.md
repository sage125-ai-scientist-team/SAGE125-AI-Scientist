# T01 Wave C — 2026-08-07 发布联调交付

**Requirements:** T01-C-004 / T01-C-005 / T01-C-006  
**PR status:** 保持 Draft（08/08 前不 Ready）

## Deliverables

| Item | Path |
|---|---|
| 125 输出序列化 | `app/evidence/serialization.py` |
| API 样例 | `docs/modules/T01/api_examples_wave_c.json` |
| 性能报告 | `docs/modules/T01/performance_report_wave_c.md` |
| 接口说明 | `docs/modules/T01/INTERFACE_WAVE_C.md` |
| 测试 | `tests/evidence/test_serialization.py` |

## T07 / T08 联调说明（无共享路径越权）

- **T08**：消费 `build_t08_citation_payload` / `build_output_envelope_v125` 的 JSON；本 PR 不修改 `docs/modules/T08/**`。
- **T07**：可读取信封中的 `deterministic_digest` 做批处理幂等键；本 PR 不修改 `app/batch/**` 或 T07 路径。
- 跨模块冲突：仅通过冻结契约与 JSON 信封交接；无未登记共享文件修改。

## Reproduce

```powershell
python -m pytest -q tests/evidence/test_serialization.py
python -c "from app.contracts.evidence import EvidenceCardContract, ClaimEvidenceLink, EvidenceBundle; from app.evidence.citation_renderer import build_citation_item; from app.evidence.quality_gate import run_quality_gate; from app.evidence.serialization import build_output_envelope_v125, dumps_output_envelope; c=EvidenceCardContract(evidence_id='EV1',source_id='s',source_type='paper',title='T',quoted_text='Quote body for envelope demo path.',locator={'page':1},authors=['A'],year=2024,doi='10.1/x',content_hash='sha256:x',domain='medicine'); b=EvidenceBundle(bundle_id='B1',evidences=[c],links=[ClaimEvidenceLink(claim_id='C1',evidence_id='EV1',relation='supports',claim_domain='medicine')]); print(dumps_output_envelope(build_output_envelope_v125(bundle=b,citations=[build_citation_item(claim_id='C1',card=c)],quality=run_quality_gate(b)))[:400])"
```
