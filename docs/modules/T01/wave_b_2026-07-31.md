# T01 Wave B — 07/31 支持关系判定

## Safety / non-interference

| Guard | Status |
|---|---|
| 仅在分支 `t01/b-evidence-core` 提交 | Yes |
| 不修改 `t01/a-evidence-contract` / 不 force-push A | Yes |
| 不修改已冻结 `app/contracts/evidence.py` | Yes（检查器放在 `app/evidence/`） |
| 不修改 `app/workflow/pipeline.py` | Yes |
| Wave A 契约单测继续通过 | Required evidence |

## Handbook requirement

实现 claim-to-evidence 绑定、证据 ID 存在性、metadata-only 拒绝、问题册排除、语义不蕴含与跨域外推检查。

交付：支持检查器、错误码和失败夹具。  
验收：至少 5 类坏引用被阻断；不确定项被降级而非伪装通过。

## Implementation

| Path | Role |
|---|---|
| `app/evidence/support_checker.py` | `check_claim_evidence_support` + 错误码 |
| `tests/evidence/test_support_checker.py` | ≥5 类坏引用夹具 |

### Error codes

| Code | Decision |
|---|---|
| `UNKNOWN_EVIDENCE_ID` | BLOCK |
| `METADATA_ONLY` | BLOCK |
| `BOOKLET_EXCLUDED` | BLOCK |
| `UNSUPPORTED_CLAIM` | BLOCK（supports 无绑定） |
| `CROSS_DOMAIN` | DEGRADE |
| `NON_ENTAILMENT` | DEGRADE |

## Verify

```powershell
python -m pytest -q tests/evidence -vv
```
