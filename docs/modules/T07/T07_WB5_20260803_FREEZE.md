# T07-WB5 five-run freeze record

Freeze ID: `T07-WB5-20260803-v1`

Freeze date: 2026-08-03 (Asia/Shanghai)

Scope: pre-run engineering only. No provider preflight and no five-question run were executed while producing this record.

## Authoritative inputs

| Input | Frozen path | Size | SHA-256 | Current verification |
|---|---|---:|---|---|
| Authoritative booklet | `data/raw/sjtu-booklet.pdf` | 8,422,081 | `4bda50e8e3c90f8968f1bfd72ded4d9587ae80cd40ba66656a12c93abcf8e576` | blocked: file absent |
| Production 125-question JSON | `data/processed/questions_125.json` | 105,068 | `b6712a3b53f9776d7f695ea67f810c30b7d97ee59c183009432870d3224cdebb` | blocked: file absent |

The authoritative JSON is read-only. Mapping is exclusively `question_id = record["id"]`; array indexes and ordering are never used to infer IDs. Synthetic fixtures are not a fallback.

## Frozen five-question selection

| Question ID | Domain | Complete question | Canonical input hash | Mapping status |
|---|---|---|---|---|
| `Q001` | not evaluated | not evaluated | not evaluated | authoritative source missing |
| `Q028` | not evaluated | not evaluated | not evaluated | authoritative source missing |
| `Q050` | not evaluated | not evaluated | not evaluated | authoritative source missing |
| `Q075` | not evaluated | not evaluated | not evaluated | authoritative source missing |
| `Q107` | not evaluated | not evaluated | not evaluated | authoritative source missing |

The missing values above are deliberate fail-closed records, not empty approvals. Domain, complete question text, and canonical hashes cannot be frozen truthfully until the exact authoritative JSON is present and its size and SHA-256 pass. The machine-readable config preserves these fields as `null` with an explicit unverified mapping status; preflight rejects them.

## Provider, route, and models

- Provider: `bailian`
- Route ID: `t07-wb5-bailian-qwen-stack-v1`
- Fast: `qwen3.6-flash`
- Balanced: `qwen3.7-plus`
- Strong: `qwen3.7-max`
- Deep research: `qwen-deep-research`
- Embedding: `text-embedding-v4`
- Rerank: `qwen3-rerank`
- Model version: `qwen-stack-20260803-v1`

Provider configuration is reduced to a boolean. No key or authorization value is stored or emitted.

## Prompt and schema freeze

- Prompt version: `sage125-agent-prompts-20260803-v1`
- Frozen prompt path: `app/agents/prompts.py`
- Frozen prompt SHA-256: `b1afe045af8233f6255e4e1f2dc22645f88a0bae6ebf26b89028b3f2b383c3e0`
- Current file SHA-256: `fa2d1da7d40ad6a6da800d6a41484973b46b63ebe72ed48da44b437644a5c808`
- Current verification: blocked (`CODE_FILE_SHA256_MISMATCH`)
- Batch schema: `t07.batch.v1`
- Checkpoint schema: `t07.checkpoint.v1`

Frozen schema surfaces:

| Name | Path | Size | SHA-256 |
|---|---|---:|---|
| Core schemas | `app/core/schemas.py` | 15,743 | `eece52a348093213aad134642a5f9dd611d6ebaa560a9259f93beea8e9c670f5` |
| Agent schemas | `app/core/agent_schemas.py` | 12,951 | `216204b1325439e6b55fb0fb73955dd6584a22d8a2853a120ef805bfda37605b` |
| Evidence contract | `app/contracts/evidence.py` | 9,467 | `d954ae5d023faf67b2aa564113e12a57ccee45b338633fc7d21e16a5887cd7c9` |
| Validation contract | `app/contracts/validation.py` | 44,304 | `961cfbdbbd291315cc62e51931d38f060a0cac911ac468a27e86444e98c49152` |

The static prompt-file hash does not replace the dynamic prompt hash recorded for every actual call.

## T01/T03 interface freeze

- Approved T01 commit: `a4bba2e0b479d5dc0affdf5c2adc4307caed3ec7`
- Current availability: blocked; the commit is not a Git object in this checkout and therefore cannot be proven an ancestor of HEAD.
- Required T01 API: `app.evidence.precheck_bundle_for_validation`
- Current availability: blocked; `app.evidence` is absent.
- Required T03 APIs: `ValidationContext.model_validate`, `GateResult.from_legacy`, `ValidationReport.from_context`, and `run_all_quality_gates`.
- Current T03 availability: available on the current HEAD.

No T01 code is copied or cherry-picked by this work.

## Budget freeze

| Scope | Token limit | Cost limit (USD) |
|---|---:|---:|
| Per question | 200,000 | 3.00 |
| Five-question batch | 1,000,000 | 15.00 |

Maximum output per call is 8,192 tokens. Arithmetic uses `Decimal`. Preflight and retries are charged, resume is idempotent by sanitized request ID, and unknown cost fails closed. `BUDGET_EXHAUSTED` is stable. No unfrozen real-time price is hard-coded; a price snapshot must include version, source, and acquisition time.

## Completion rule

Every question begins at `gates_pending`. It may become `completed` only when all 20 frozen conditions pass: authoritative source, frozen mapping, eight identity/provenance bindings, actual T03 context, T01, T03, no open P0/P1, five required artifacts, call audit, audit truth/cost, artifact-manifest integrity, delivery-index integrity, and both budget levels.

The five minimum artifacts remain `report.pdf`, `report.md`, `result.json`, `evidence_cards.json`, and `agent_trace.json`. This freeze additionally requires `llm_call_audit.json` in both the artifact manifest and delivery index. It must record provider, exact model, route tier, timestamp, sanitized request ID, static and dynamic prompt hashes, tokens, cost, retry, and `fallback=false`, with no secret material.

## Current frozen status

- `FIVE_REAL_RUNS_BLOCKED`
- `T01_GATE_VERSION_UNAVAILABLE`
- `PROVIDER_PREFLIGHT_NOT_EXECUTED`

Additional blockers: authoritative files absent, five mappings not evaluated, frozen prompt hash mismatch, provider configuration not disclosed to this audit, no price snapshot, and a dirty worktree while this uncommitted implementation is under review.
