# T06 Wave B Evaluation Report (PR-B)

## Scope and honesty labels

| Kind | Status |
| --- | --- |
| Evaluation kind | `synthetic_fixture_offline` |
| Zenodo / PR #29 real gold | **BLOCKED** — package not present on `integration/2026-08-10` base; T06 did **not** copy PR #29 assets |
| Paid Qwen / Bailian calls | **NOT PERFORMED** (`EXTERNAL_PAID_CALLS_PERFORMED=NO`) |
| Mock claiming actual | **NO** |

PR #29 fixed Head `7427a5a2773f293903bc6fa530294aebb408d661` remains frozen and unmodified. T09 provenance-only acceptance does **not** apply to this PR-B.

## Dataset

- Manifest: `tests/multimodal/fixtures/wave_b/GOLD_MANIFEST.json`
- Sample size: 3 cases (table / chart / timeseries)
- Kind: `synthetic_fixture` only

## Reproduction

```powershell
$env:PYTHONPATH = (Get-Location).Path
python -X utf8 -m app.multimodal.eval_metrics
python -X utf8 -m pytest -q tests/multimodal
```

Machine-readable metrics: `docs/modules/T06/wave_b/metrics.json` (regenerated by the command above).

## Thresholds and results

- Threshold: cell accuracy ≥ 0.99 on synthetic gold
- See `metrics.json` → `overall` / `by_modality` / per-case errors
- Failures are typed (`ExtractionError`, etc.); no silent inventing of cells or units

## EvidenceCard / E2E

- Bridge: `app/multimodal/evidence_bridge.py` → `EvidenceCardContract` (consumes integration T01 contract; does not modify T01 paths)
- Hook: `app/multimodal/workflow_hook.py` (T06-owned revision summary payload; `binary_in_prompt=false`)
- Minimal E2E covered in `tests/multimodal/test_skeleton.py::test_minimal_e2e_queue_evidence_and_hook`

## Audit / security / limits

- Vision audit never stores keys or full prompts
- Tokens/cost fields only legal for actual external calls; denied path leaves them null
- Known limits: no real PDF OCR; structured packets only; real gold blocked until #29 merges or captain authorizes asset promotion

## Ready / merge

This PR-B remains **Draft**. Ready authority is captain-only after T05 pairing review and compliance gates.
