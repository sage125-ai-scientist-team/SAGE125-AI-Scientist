# Streamlit Custom Components v2 组件清单。
# Streamlit 运行时会在启动阶段读取本文件以定位构建产物目录（asset_dir）。
[project]
name = "sage125-landing"
version = "0.1.0"

[[tool.streamlit.component.components]]
name = "sage125_landing"
asset_dir = "frontend/build"

[[tool.streamlit.component.components]]
name = "sage125_scroll"
asset_dir = "frontend/build"
