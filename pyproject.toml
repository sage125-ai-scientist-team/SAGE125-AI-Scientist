[project]
name = "sage125-ai-scientist"
version = "0.1.0"
description = "Evidence-grounded multi-agent AI Scientist for the 125 Questions booklet"
requires-python = ">=3.10,<3.15"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
cache_dir = "data/cache/pytest-cache"

